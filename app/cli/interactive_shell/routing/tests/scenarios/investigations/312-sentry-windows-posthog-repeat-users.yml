id: 312-sentry-windows-posthog-repeat-users
title: PostHog Windows retention issue asks Sentry for likely cause
intent_class: investigation
risk_level: medium
input:
  prompt: >-
    From Posthog we can see that the OpenSRE application is not working well for
    Windows users. We do not have many repeat users. Can you check our Sentry
    what the issue is on windows?
  surface: interactive_cli
session:
  has_prior_state: false
  remote_connected: false
  configured_integrations: []
available_capabilities:
  slash_commands: []
  cli_commands: []
  synthetic_suites: []
notes:
- "Covers the default assistant behavior for a product-analytics-to-error-tracking\
  \ diagnostic question. The agent should answer directly and avoid shelling out\
  \ or starting an RCA run without a configured Sentry context."
route:
  expected_kind: handle_message_with_agent
  expected_signals: []
  expected_command_text: null
policy:
  should_execute: false
  has_unhandled_clause: true
  fail_closed: true
planned_actions: []
executed_actions: []
response_contract:
  must_contain_any:
  - Sentry
  - Windows
  - repeat users
  - PostHog
  must_not_contain:
  - $ /
  forbidden_actions:
  - shell
  - investigation
history:
  expected:
  - type: cli_agent
    ok: true
tier: full
runs: 1
