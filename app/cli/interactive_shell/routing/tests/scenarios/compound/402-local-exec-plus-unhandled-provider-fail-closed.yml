id: 402-local-exec-plus-unhandled-provider-fail-closed
title: Supported slash action combined with unsupported LLM provider request, fail-closed
intent_class: compound
risk_level: high
input:
  prompt: show me connected services and connect me to local llama
  surface: interactive_cli
session:
  has_prior_state: false
  remote_connected: false
  configured_integrations: []
available_capabilities:
  slash_commands:
  - /list
  cli_commands: []
  synthetic_suites: []
notes:
- Supported /list integrations action combined with an unsupported local-LLM connection
  request. The unhandled provider request prevents safe execution of the valid action.

route:
  expected_kind: cli_agent
  expected_signals: []
  expected_command_text: null
policy:
  should_execute: false
planned_actions: []
executed_actions: []
response_contract:
  must_contain_any:
  - couldn't safely decide actions
  must_not_contain:
  - $ /
history:
  expected:
  - type: cli_agent
    ok: false
tier: critical
runs: 1
