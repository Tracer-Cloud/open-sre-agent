id: 404-multi-step-plus-nonsense-fail-closed
title: Multi-step valid slash actions plus nonsense request, fail-closed
intent_class: compound
risk_level: high
input:
  prompt: check system health, list connected integrations, and order me a pizza
  surface: interactive_cli
session:
  has_prior_state: false
  remote_connected: false
  configured_integrations: []
available_capabilities:
  slash_commands:
  - /health
  - /list
  cli_commands: []
  synthetic_suites: []
notes:
- Two slash commands that would execute cleanly on their own (/health, /list integrations)
  are blocked by an out-of-scope pizza order. Verifies that a multi-step sequence
  with any unsupported clause still fails closed.

route:
  expected_kind: cli_agent
  expected_signals: []
  expected_command_text: null
policy:
  should_execute: false
planned_actions: []
executed_actions: []
response_contract:
  must_contain_any:
  - couldn't safely decide actions
  must_not_contain:
  - $ /
history:
  expected:
  - type: cli_agent
    ok: false
tier: critical
runs: 1
