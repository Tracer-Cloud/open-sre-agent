id: 321-priority-prompt-database-slow
title: Priority prompt — why is the database slow
intent_class: complex_shell_prompts
input:
  prompt: 'why is the database slow?'
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- P0 priority prompt (misc_real_investigation #1) from OpenSRE analytics priority-prompts report.
- Prompt is sent verbatim; do not normalize typos or mixed language.
route:
  expected_kind: handle_message_with_agent
  expected_command_text: null
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: diagnostic_question:database_slow
  source: llm
executed_actions: []
response_contract:
  must_contain_any:
  - database
  - slow
  - incident
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
gathered_tools_contract:
  must_not_call:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
