id: 328-priority-prompt-sniffdog-investigate-service
title: Priority prompt — sniffdog investigate service (CLI-shaped)
intent_class: complex_shell_prompts
input:
  prompt: sniffdog investigate --service myportfolio
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
available_capabilities:
  cli_commands:
  - investigate
notes:
- P0 priority prompt (kubernetes_workload_health #2) from OpenSRE analytics priority-prompts report.
- Prompt is sent verbatim (sniffdog is intentional non-canonical CLI phrasing).
- Live planner typically maps this to cli_command investigate --service myportfolio rather than
  investigation_start or a pure assistant_handoff. Planning may echo the sniffdog prefix in the
  payload; executed payload is normalized without the opensre prefix.
route:
  expected_kind: handle_message_with_agent
  expected_command_text: null
policy:
  executes_terminal_action: true
planned_actions:
- kind: cli_command
  source: llm
  target_surface: terminal
  payload: investigate --service myportfolio
executed_actions:
- kind: cli_command
  payload: investigate --service myportfolio
response_contract:
  must_contain_any:
  - investigat
  - sniffdog
  - myportfolio
  - service
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
  forbidden_actions:
  - investigation
gathered_tools_contract:
  must_not_call:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
history:
  expected:
  - type: cli_agent
    ok: true
  - type: cli_command
    ok: true
runs: 3
