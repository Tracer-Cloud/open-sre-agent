id: 327-priority-prompt-k8s-cpu-spike
title: Priority prompt — Kubernetes CPU spike
intent_class: complex_shell_prompts
input:
  prompt: 'High CPU spike in Kubernetes cluster'
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- P0 priority prompt (kubernetes_workload_health #1) from OpenSRE analytics priority-prompts report.
- Prompt is sent verbatim; do not normalize typos or mixed language.
route:
  expected_kind: handle_message_with_agent
  expected_command_text: null
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: incident_description:k8s_cpu_spike
  source: llm
executed_actions: []
response_contract:
  must_contain_any:
  - cpu
  - kubernetes
  - cluster
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
gathered_tools_contract:
  must_not_call:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
