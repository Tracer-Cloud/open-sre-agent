id: 326-priority-prompt-checkout-500-latency-deploy
title: Priority prompt — checkout 500s and latency after deploy
intent_class: complex_shell_prompts
input:
  prompt: 'checkout-api has elevated 500s and latency after deploy'
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
notes:
- P0 priority prompt (http_5xx_or_latency_after_deploy #2) from OpenSRE analytics priority-prompts report.
- Prompt is sent verbatim; do not normalize typos or mixed language.
route:
  expected_kind: handle_message_with_agent
  expected_command_text: null
policy:
  executes_terminal_action: false
planned_actions:
- kind: assistant_handoff
  content: incident_description:checkout_500_latency_deploy
  source: llm
executed_actions: []
response_contract:
  must_contain_any:
  - checkout
  - "500"
  - latency
  - deploy
  must_not_contain:
  - $ /
  - can't help
  - cannot help
  - I can't help
  - I cannot help
gathered_tools_contract:
  must_not_call:
  - search_sentry_issues
  - search_github_issues
  - list_posthog_tools
history:
  expected:
  - type: cli_agent
    ok: true
runs: 3
