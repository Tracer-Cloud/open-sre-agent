"""Deployment-facing runtime packages.

This top-level package contains code that runs at deployment boundaries:
health polling and local persisted EC2 outputs.

Import from ``infra.deployment.operations``.
"""
